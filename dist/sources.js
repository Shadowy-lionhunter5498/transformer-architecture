import {facts} from './facts.js';
export const sourceHTML=`<h3>Two primary sources</h3><p><a href="${facts.sources.original}" target="_blank" rel="noopener">Attention Is All You Need · Vaswani et al., 2017 ↗</a><br>Figure 1, §3 and Table 3. This app uses the English-to-German base configuration reported as 65M parameters in Table 3. It is a sequence-to-sequence model; the decoder consumes a separate, shifted target sequence.</p><p style="margin-top:12px"><a href="${facts.sources.report}" target="_blank" rel="noopener">DeepSeek-V4.1-Flash: Pushing the Limits of KV Cache Compression ↗</a><br>Released 10 September 2026. Figures 3–5; §2, §3.2.2 and §4.2.1. Checked against the <a href="${facts.sources.config}" target="_blank" rel="noopener">official configuration ↗</a>.</p>
<h3>What changed</h3><table><thead><tr><th>Component</th><th>Original base</th><th>V4.1 Flash</th></tr></thead><tbody><tr><td>Stack</td><td>6 bidirectional encoder + 6 causal decoder</td><td>20 causal encoder + 20 causal decoder</td></tr><tr><td>Attention</td><td>8 heads × 64 dimensions; dense</td><td>64 query heads × 512; one shared KV latent per entry; CSA2 + local SWA</td></tr><tr><td>Token width</td><td>512</td><td>5,120</td></tr><tr><td>Feed-forward</td><td>Dense, ReLU, width 2,048</td><td>40-layer backbone: 6/384 routed + 1 shared; clamped SwiGLU, width 2,304 per expert</td></tr><tr><td>Residual / norm</td><td>One stream; post-LayerNorm</td><td>Four streams; Single-Pass mHC + RMS pre-norm</td></tr><tr><td>Embedding weights</td><td>Input, output and pre-softmax projection tied; embeddings scaled by √512</td><td>Input embeddings and output head untied</td></tr><tr><td>Position</td><td>Added sinusoids</td><td>Rotary position encoding; YaRN context extension</td></tr><tr><td>Extra pathways</td><td>Text embeddings</td><td>Vision, Engram conditional memory, DSpark</td></tr></tbody></table>
<h3>Exact layer schedule · zero-based</h3><p>0–1: local SWA only. Encoder Full layers 2, 8, 14, each followed by five Reuse layers, at 2:1 compression. Decoder Full layer 20; Reindex at 24, 28, 32, 36; all remaining decoder layers Reuse, at 1:1 compression. Every layer retains its own query and local KV. Full computes global KV and fresh indices. Reindex shares global KV but computes new indexer queries and indices. Reuse shares both global KV and the latest indices and has no indexer computation. There are 32 indexer query heads, each 128 dimensions, distinct from the 64 main query heads. Full means full CSA2 computation, not dense core attention. RoPE acts within attention, rather than being added as a second input embedding.</p>
<h3>The details that matter</h3><p>552B is the backbone; Engram adds 196B sparsely accessed parameters across two modules at zero-based layers 1 and 14. Each module has n-gram orders 2, 3 and 4 with eight hash heads per order: 24 tables per module, 48 total. Each table holds approximately 16M rows. The close-up shows one illustrative addressed row per table. The overview edges target groups containing those specific layers. Engram uses context-aware gating and multi-branch integration; its deterministic addressing is not a search of conversation KV. Vision uses a 32-layer encoder (1,024 dimensions, 16 heads), 14-pixel patches, 3×3 unshuffle (9× fewer visual positions), and a two-layer projector to 5,120 dimensions. It has no equivalent input path in the 2017 model.</p><p style="margin-top:12px">For an uncached prompt, a simplified prefill proxy is 20N + 20·min(N,128) token-layer visits. This excludes projection, indexing, vision and Engram costs and is not FLOPs. Its reduction approaches one half only for N much larger than 128. Encoder prefix-cache misses can require their own bounded replay. The last 128 prompt tokens replay through the decoder to approximately reconstruct local KV; these are not mathematically exact full-context states. Decode runs both halves. The first decoder indexer still scores the full causal context; later indexers search at most 16,384 candidates and select at most 512. Counts shrink when fewer causal entries exist. Reindex selections stay within the candidate pool created by the first decoder indexer. A fixed-size local window does not make all indexing constant-cost.</p>
<h3>Scale and speed, honestly</h3><p>The 890 bytes/token figure is reported global KV only, excluding bounded local SWA, weights and runtime overhead. Cache-bank volumes follow the three 2:1 encoder banks and one 1:1 decoder bank. Per full entry, the 512-channel main KV uses 256 payload bytes plus 32 scale bytes; the 128-channel indexer K uses 64 payload bytes plus four scale bytes. Thus (288 + 68) × (½ + ½ + ½ + 1) = 890 bytes per original token. Main KV uses E2M1 with an E4M3 scale per 16 channels; indexer K uses MXFP4, and local SWA KV remains FP8. Encoder hidden states are a separate object, not the 890-byte cache. Original cache bars assume 2-byte values and compute N × 6 decoder layers × 2 (K,V) × 512 × 2 bytes; target self-attention cache is excluded. The 2017 paper does not report that BF16 cache layout or a 1M-token capability. The slider extrapolates a storage formula. These are different cache contents; they cannot establish an overall memory or speed advantage. Neither number includes model weights, temporary tensors or serving overhead.</p><p style="margin-top:12px">V4.1 reports ~4× less global KV and ~8× less persistent cache than V4 Flash under the report's deployment conditions, not than the original Transformer. Its precision-weighted decode FLOPs grow about 25% from 4K to 1M context (§1, Figure 2). No comparable tokens/second benchmark against the 2017 model is supplied. DSpark is an optional drafting branch, separate from the backbone output head and outside its 40 layers. Three draft blocks with SWA 128 produce base logits for five positions in parallel; a Markov head couples the drafts. A confidence head and a scheduler informed by engine throughput/load choose verification length. The target backbone performs verification. The close-up uses an illustrative three-of-five accepted prefix; rejected candidates stop at the gate, and correction / bonus tokens are omitted. This example is not a reported acceptance rate or a speedup measurement. Animation pace is a viewing control.</p>
<h3>What you are seeing</h3><p>The spatial housings, repeat enclosures and pathways were authored in Blender. Their layout follows the original Figure 1 and DeepSeek Figure 3. Scroll or pinch toward a component to reveal its contents; clicking moves the camera to the same existing component. The housings recede and their internals expand in place as you approach. Repeated enclosures follow the papers' nested repeat notation. They indicate repeated layer structures with separate weights, not weight sharing or additional layers. The overview omits the internal projection chain and per-sublayer norm/residual plumbing. In DeepSeek, four residual streams and RMS pre-norm apply around sublayers throughout the backbone; Single-Pass mHC uses the preceding block's input-mixing coefficients. The input-stream glyph is not a second embedding lookup.</p><p style="margin-top:12px">Token traffic is a continuous schematic of the available pathways, not a prefill/decode timeline or measured inference. Glyphs represent grouped activations and copies on Q/K/V and residual branches, not a literal token count. The input slider changes traffic density and cache volume. Matrices are coarse access maps, not attention weights, literal token counts or measured sparsity ratios. The global-map groups and local-KV glyphs have different meanings; one global entry represents two tokens in the encoder and one in the decoder. The indexer and cache-source states follow the real layer schedule, while example selections are synthetic. The candidate-pool close-up uses 64 equal-sized groups; its highlighted fraction follows the selected/candidate entry budget, but does not identify actual selected positions. The matrix has 16 × 16 representative groups; the head counts (8 and 64), routed expert count (384, with 6 active), shared expert, and embedding dimensions are factual. Each original feed-forward tile represents 64 hidden units. All are evaluated in the dense FFN, but ReLU can zero their outputs. The internal geometries and animated selections are explanatory. Eight selected original position dimensions use the paper's paired sine/cosine formula and different frequencies. Output-head bars show 16 toy logits or normalized illustrative probabilities, not the full vocabulary or model predictions. Apart from explicitly labelled counts and cache volumes, housing size and animation speed are not parameter, FLOP or latency scales. Timing and motion speed are viewing controls, not a tokens/second benchmark.</p><h3>Evidence boundary</h3><p>This presentation checks the architecture and formulas against the cited papers and the saved official configuration. It does not run either neural model or independently reproduce performance. DeepSeek reports little degradation from bounded replay, but §6 explicitly leaves robustness under extreme retrieval and cache-resumption conditions unresolved. Its benchmark results and broad capability statements do not prove correctness on arbitrary tasks. The 2017 translation system and the modern causal language model are not matched performance baselines.</p>`;
